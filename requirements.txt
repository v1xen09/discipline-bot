# Telegram
python-telegram-bot==21.6

# Локальная LLM через LM Studio (OpenAI-совместимый сервер).
# Сам OpenAI SDK мы используем только как клиент HTTP — никакие
# запросы на api.openai.com не уходят, base_url переопределяется.
openai>=1.40.0

# Локальное распознавание речи (Whisper).
# CTranslate2 + PyAV ставятся как зависимости автоматически.
faster-whisper>=1.0.0

# Графики продуктивности (week / month / today)
matplotlib>=3.8

# Database
aiosqlite>=0.20.0

# Scheduler
APScheduler>=3.10.4

# Env vars
python-dotenv>=1.0.0

# REST API (для десктопного клиента)
flask[async]>=3.0.0
flask-cors>=4.0.0
